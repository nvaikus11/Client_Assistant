# Shared dependencies for the AI Tools Workspace.
# Each tool may add its own requirements.txt for tool-specific needs.

# Anthropic API SDK — the only model provider in this workspace.
anthropic>=0.40.0

# Load ANTHROPIC_API_KEY (and other config) from .env at runtime.
python-dotenv>=1.0.0

# Document ingestion for SOW / RFP / decks (used by shared/doc_parsing.py).
pypdf>=4.0.0            # PDF text extraction
python-docx>=1.1.0      # .docx (Word) parsing
python-pptx>=0.6.23     # .pptx (PowerPoint) parsing

# Local UI (ui/app.py) — runs on localhost only, no cloud.
streamlit>=1.36.0
